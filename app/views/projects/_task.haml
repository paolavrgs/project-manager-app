%li.d-flex.justify-content-between.mb-3
  .task-title
    %input.task-checkbox.styled-checkbox{id: "task-#{task.id}", type: "checkbox", checked: task.status}
    %label{ for: "task-#{task.id}"}= task.title
  .task-priority
    = link_to edit_task_path(task) do
      %i.fa.fa-pencil
    = link_to task, method: :delete, data: { confirm: '¿Seguro que desea eliminar?' } do
      %i.fa.fa-trash
