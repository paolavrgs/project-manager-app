%p#notice= notice
%h1 Tasks
%table
  %thead
    %tr
      %th Title
      %th Priority
      %th Description
      %th{:colspan => "3"}
  %tbody
    - @tasks.each do |task|
      %tr
        %td= task.title
        %td= task.priority
        %td= task.description
        %td= link_to 'Show', task
        %td= link_to 'Edit', edit_task_path(task)
        %td= link_to 'Destroy', task, method: :delete, data: { confirm: 'Estás seguro?' }
%br/
= link_to 'New Task', new_task_path
